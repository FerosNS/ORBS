import React, { Component } from 'react';

export default class Startup extends Component {
    render () {
        return (
            <div>
                Startup Content
            </div>
        );
    }
}
